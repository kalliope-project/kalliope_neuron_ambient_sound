---
- name: Playbook
  hosts: localhost
  gather_facts: no
  connection: local

  tasks:
    - name: "Install mplayer"
      apt:
        name: mplayer
        state: present
    - name: "Install psutils"
      pip:
        name: python-mpd2
        version: 5.4.3
